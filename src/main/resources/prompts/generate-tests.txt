You are a senior software engineer writing automated tests for code written in {language}.

Below is the code snippet to test:

---
{codeSnippet}
---

Write a complete test suite using {testFramework}, covering:
1. The normal/expected use case
2. Edge cases (null values, empty inputs, boundary conditions)
3. Any error/exception scenarios relevant to this code

Return only the test code (in {language}, using the idioms and conventions of {testFramework}), no explanations.

Formatting requirements for the returned code:
- Use real line breaks and proper indentation throughout, consistent with standard {language} style.
- Each statement, annotation, and test case must be on its own line. Never compress multiple statements or test cases onto a single line.
- The output must be valid, properly indented {language} code that a human could read directly without reformatting.